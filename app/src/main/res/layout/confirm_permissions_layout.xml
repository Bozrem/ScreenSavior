<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <FrameLayout
        android:id="@+id/headerContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Large Text Element Positioned Above Center -->
    <TextView
        android:id="@+id/largeText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_marginBottom="8dp"
        android:text="In order to function correctly, ScreenSavior needs access to Overlay and Usage Monitoring permissions"
        android:textSize="24sp"
        android:textAlignment="center"
        android:textStyle="bold" />

    <!-- Button Below Large Text Element -->
    <Button
        android:id="@+id/enterPermissionsButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/largeText"
        android:layout_centerHorizontal="true"
        android:text="ENTER PERMISSIONS" />
</RelativeLayout>